<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Telesma" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/telesma_accent</item>
        <item name="colorPrimaryVariant">@color/telesma_accent_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/telesma_background</item>
        <item name="android:navigationBarColor">@color/telesma_background</item>
        <!-- Window background -->
        <item name="android:windowBackground">@color/telesma_background</item>
    </style>
</resources>
